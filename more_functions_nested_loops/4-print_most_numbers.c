#include <stdio.h>
#include "main.h"

/**
 * print_numbers  - the function that multiplicates
 */
void print_numbers(void)
{
	int a;

	for (a = '0'; a <= '9'; a++)
		{
			if (a != '2' && '4')
				_putchar(a);
		}
				_putchar('\n');
}
