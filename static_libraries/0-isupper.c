#include <stdio.h>
#include "main.h"

/**
 * _isupper - the function that checks for uppercase letters
 * @c: the parameter
 * Return: Always 0.
 */

int _isupper(int c)

{

	return ((c >= 'A' && c <= 'Z'));

}
