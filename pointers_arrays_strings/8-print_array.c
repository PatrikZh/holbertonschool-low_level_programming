#include <stdio.h>
#include "main.h"

/**
 * print_array - the function
 * @a: the given value
 * @n: the given value
 */

void print_array(int *a, int n)
{
	int i;

	for (int i = 0; i < n; i++)
	{
		printf("%d\n", n);
	}
	printf('\n');
	printf(" , ");
	printf(" ");
}
