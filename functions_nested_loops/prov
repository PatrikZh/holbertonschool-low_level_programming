#include <stdio.h>

void times_table(void)
{
        int i, j;
        int result [i] [j];
        for (i = 0; i <= 9; i++)
	for (j = 0; j <= 9; j++)
	int result [i] [j] = i*j;

	for (i = 0; i <= 9; i++)
	{
	for (j = 0; j <= 9; j++)
	printf("%5d", result [i] [j];)
	printf("\n");
	}
	return 0;
}
