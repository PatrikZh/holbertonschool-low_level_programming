#include <stdio.h>
#include "main.h"

/**
 * mul  - the function that multiplicates
 * @a: the given components
 * @b: the given components
 * Return: Always 0.
 */
int mul(int a, int b)
{
	return (a * b);

}
