#include <stdio.h>
#include "main.h"

/**
 * _isdigit - the function that checks for a digit*s
 * @c: the parameter
 * Return: Always 0.
 */

int _isdigit(int c)

{

	return ((c >= '0' && c <= '9'));

}
